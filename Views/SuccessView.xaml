<UserControl x:Class="password.Views.SuccessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:password.Views"
             xmlns:viewModels="clr-namespace:password.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewModels:SuccessViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="{Binding Name, Mode=TwoWay}" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" />
        <TextBlock Text="You have successfully registered" FontSize="14" FontWeight="Normal" HorizontalAlignment="Center" />
    </StackPanel>
</UserControl>
